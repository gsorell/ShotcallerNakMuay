{
  "devDependencies": {
    "@types/node": "^24.3.1",
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "dev": "npm --prefix client run dev",
    "start": "npm --prefix client run start",
    "build": "npm --prefix client run build",
    "test": "npm --prefix client test",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d client/dist -t true"
  },
  "dependencies": {
    "nosleep.js": "^0.12.0"
  }
}
